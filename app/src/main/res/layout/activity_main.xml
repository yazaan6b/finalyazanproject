<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- أيقونة الحصان -->
    <ImageView
        android:id="@+id/horse_icon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_centerInParent="true"
        android:padding="10dp" />

    <!-- النص العربي -->
    <TextView
        android:id="@+id/tvtextarabic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/horse_icon"
        android:layout_centerHorizontal="true"
        android:text="الخُيول العربيّة"
        android:textColor="@android:color/white"
        android:textSize="26sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        android:shadowColor="#AA000000"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="4" />

    <!-- النص الإنجليزي -->
    <TextView
        android:id="@+id/tvtextenglish"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvtextarabic"
        android:layout_centerHorizontal="true"
        android:text="Arabian Horses"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:layout_marginTop="8dp"
        android:shadowColor="#88000000"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="4" />

</RelativeLayout>

